import stack.TextEditor;

public class Main {
    public static void main(String[] args) {
        System.out.println("\n=== SIMULASI STACK (Undo/Redo) ===");
        TextEditor editor = new TextEditor();
        editor.runEditor();
    }
}
